services:
  - type: web
    name: appointment-reminder
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
      - key: APP_DEBUG
        value: false
